<div class="page-container">
  <div class="page-header">
    <a href="/projects/<%= project._id %>" class="back-link">
      <i data-lucide="arrow-left"></i>
      Retour au projet
    </a>
    <div class="page-header-flex">
      <div>
        <h1>Modifier le projet</h1>
        <p class="text-muted">Mettez à jour les informations de votre projet</p>
      </div>
      <a href="/projects/<%= project._id %>" class="btn btn-secondary">
        <i data-lucide="wand-sparkles"></i>
        Outils IA
      </a>
    </div>
  </div>

  <form action="/projects/<%= project._id %>?_method=PUT" method="POST" enctype="multipart/form-data" class="project-form">

    <!-- Step 1: Basic Info -->
    <div class="form-section">
      <h2><span class="step-num">1</span> Informations du projet</h2>

      <div class="form-group">
        <label class="form-label">Titre du projet *</label>
        <input type="text" name="title" class="form-input" placeholder="ex : Rénovation cuisine moderne" required value="<%= project.title %>">
      </div>

      <div class="form-group">
        <label class="form-label">Description *</label>
        <textarea name="description" class="form-textarea" rows="4" placeholder="Décrivez votre vision pour cet espace..." required maxlength="1000"><%= project.description %></textarea>
        <p class="form-hint">Soyez aussi précis que possible — couleurs, styles, matériaux et besoins spécifiques</p>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label class="form-label">Type de pièce *</label>
          <select name="roomType" class="form-select" required>
            <option value="">Sélectionner une pièce</option>
            <% if (typeof roomTypes !== 'undefined') { %>
              <% roomTypes.forEach(room => { %>
                <option value="<%= room.id %>" <%= project.roomType === room.id ? 'selected' : '' %>><%= room.name %></option>
              <% }) %>
            <% } %>
          </select>
        </div>

        <div class="form-group">
          <label class="form-label">Style de design</label>
          <select name="style" class="form-select">
            <option value="">Laisser l'IA décider</option>
            <% if (typeof styles !== 'undefined') { %>
              <% styles.forEach(s => { %>
                <option value="<%= s.id %>" <%= project.style === s.id ? 'selected' : '' %>><%= s.name %></option>
              <% }) %>
            <% } %>
          </select>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label class="form-label">Statut</label>
          <select name="status" class="form-select">
            <option value="draft"       <%= project.status === 'draft'       ? 'selected' : '' %>>Brouillon</option>
            <option value="in-progress" <%= project.status === 'in-progress' ? 'selected' : '' %>>En cours</option>
            <option value="completed"   <%= project.status === 'completed'   ? 'selected' : '' %>>Terminé</option>
            <option value="archived"    <%= project.status === 'archived'    ? 'selected' : '' %>>Archivé</option>
          </select>
        </div>

        <div class="form-group">
          <label class="form-label">Visibilité</label>
          <select name="visibility" class="form-select">
            <option value="private"     <%= project.visibility === 'private'     ? 'selected' : '' %>>Privé — Moi uniquement</option>
            <option value="contractors" <%= project.visibility === 'contractors' ? 'selected' : '' %>>Entrepreneurs — Visible aux pros</option>
            <option value="public"      <%= project.visibility === 'public'      ? 'selected' : '' %>>Public — Tout le monde</option>
          </select>
        </div>
      </div>
    </div>

    <!-- Step 2: Photos -->
    <div class="form-section">
      <h2><span class="step-num">2</span> Photos</h2>
      <p class="section-desc">Photos actuelles et téléversement de nouvelles images</p>

      <% if (project.originalImages && project.originalImages.length > 0) { %>
        <div class="existing-images">
          <label class="form-label">Images actuelles</label>
          <div class="file-preview">
            <% project.originalImages.forEach(img => { %>
              <div class="preview-item existing">
                <img src="<%= img.url %>" alt="Project image">
              </div>
            <% }) %>
          </div>
        </div>
      <% } %>

      <div class="upload-area" id="upload-area">
        <input type="file" name="images" id="images" multiple accept="image/*" class="file-input">
        <label for="images" class="upload-label">
          <i data-lucide="upload-cloud"></i>
          <span>Glissez de nouvelles images ici ou cliquez pour parcourir</span>
          <span class="upload-hint">Max 5 images, 10 Mo chacune (JPG, PNG, WebP)</span>
        </label>
      </div>
      <div id="images-preview" class="file-preview"></div>
    </div>

    <!-- Step 3: Budget -->
    <div class="form-section">
      <h2><span class="step-num">3</span> Budget <span style="font-weight:400;font-size:0.85rem;opacity:0.6;">(optionnel)</span></h2>

      <div class="form-row">
        <div class="form-group">
          <label class="form-label">Budget minimum (&euro;)</label>
          <input type="number" name="budgetMin" class="form-input" placeholder="5000" min="0" value="<%= project.budget && project.budget.min ? project.budget.min : '' %>">
        </div>

        <div class="form-group">
          <label class="form-label">Budget maximum (&euro;)</label>
          <input type="number" name="budgetMax" class="form-input" placeholder="15000" min="0" value="<%= project.budget && project.budget.max ? project.budget.max : '' %>">
        </div>
      </div>
    </div>

    <!-- Step 4: Dimensions -->
    <div class="form-section">
      <h2><span class="step-num">4</span> Dimensions <span style="font-weight:400;font-size:0.85rem;opacity:0.6;">(optionnel)</span></h2>

      <div class="form-row-3">
        <div class="form-group">
          <label class="form-label">Longueur (m)</label>
          <input type="number" name="length" class="form-input" placeholder="4.5" step="0.1" min="0" value="<%= project.dimensions && project.dimensions.length ? project.dimensions.length : '' %>">
        </div>

        <div class="form-group">
          <label class="form-label">Largeur (m)</label>
          <input type="number" name="width" class="form-input" placeholder="3.5" step="0.1" min="0" value="<%= project.dimensions && project.dimensions.width ? project.dimensions.width : '' %>">
        </div>

        <div class="form-group">
          <label class="form-label">Hauteur (m)</label>
          <input type="number" name="height" class="form-input" placeholder="2.7" step="0.1" min="0" value="<%= project.dimensions && project.dimensions.height ? project.dimensions.height : '' %>">
        </div>
      </div>
    </div>

    <!-- Step 5: Preferences -->
    <div class="form-section">
      <h2><span class="step-num">5</span> Préférences <span style="font-weight:400;font-size:0.85rem;opacity:0.6;">(optionnel)</span></h2>

      <div class="form-row">
        <div class="form-group">
          <label class="form-label">Couleurs préférées</label>
          <input type="text" name="colors" class="form-input" placeholder="Blanc, Gris, Bois naturel" value="<%= project.preferences && project.preferences.colors ? project.preferences.colors.join(', ') : '' %>">
          <p class="form-hint">Séparez par des virgules</p>
        </div>

        <div class="form-group">
          <label class="form-label">Matériaux préférés</label>
          <input type="text" name="materials" class="form-input" placeholder="Marbre, Chêne, Laiton" value="<%= project.preferences && project.preferences.materials ? project.preferences.materials.join(', ') : '' %>">
          <p class="form-hint">Séparez par des virgules</p>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label class="form-label">À inclure absolument</label>
          <input type="text" name="mustHave" class="form-input" placeholder="Îlot central, Suspensions" value="<%= project.preferences && project.preferences.mustHave ? project.preferences.mustHave.join(', ') : '' %>">
          <p class="form-hint">Éléments indispensables dans votre design</p>
        </div>

        <div class="form-group">
          <label class="form-label">À éviter</label>
          <input type="text" name="mustAvoid" class="form-input" placeholder="Moquette, Couleurs sombres" value="<%= project.preferences && project.preferences.mustAvoid ? project.preferences.mustAvoid.join(', ') : '' %>">
          <p class="form-hint">Éléments que vous ne souhaitez pas</p>
        </div>
      </div>
    </div>

    <!-- Step 6: Priorities -->
    <div class="form-section">
      <h2><span class="step-num">6</span> Priorités</h2>

      <div class="priority-sliders">
        <div class="priority-item">
          <label>Qualité</label>
          <input type="range" name="priorityQuality" min="1" max="5" value="<%= project.priorities && project.priorities.quality ? project.priorities.quality : 3 %>" class="priority-slider">
          <span class="priority-labels"><span>Économique</span><span>Premium</span></span>
        </div>

        <div class="priority-item">
          <label>Budget</label>
          <input type="range" name="priorityPrice" min="1" max="5" value="<%= project.priorities && project.priorities.price ? project.priorities.price : 3 %>" class="priority-slider">
          <span class="priority-labels"><span>Flexible</span><span>Strict</span></span>
        </div>

        <div class="priority-item">
          <label>Délai</label>
          <input type="range" name="prioritySpeed" min="1" max="5" value="<%= project.priorities && project.priorities.speed ? project.priorities.speed : 3 %>" class="priority-slider">
          <span class="priority-labels"><span>Détendu</span><span>Urgent</span></span>
        </div>
      </div>
    </div>

    <!-- DIY -->
    <div class="form-section">
      <div class="form-group">
        <div class="diy-option">
          <input type="checkbox" name="isDIY" id="isDIY" <%= project.isDIY ? 'checked' : '' %>>
          <label for="isDIY">
            <div class="diy-icon"><i data-lucide="hammer"></i></div>
            <div>
              <strong>Projet DIY</strong>
              <p>Je prévois de réaliser ce projet moi-même</p>
            </div>
          </label>
        </div>
      </div>
    </div>

    <!-- Submit -->
    <div class="form-actions">
      <a href="/projects/<%= project._id %>" class="btn btn-ghost">Annuler</a>
      <button type="submit" class="btn btn-primary btn-lg">
        <i data-lucide="save"></i>
        Enregistrer les modifications
      </button>
    </div>
  </form>
</div>

<style>
  .page-container {
    max-width: 800px;
    margin: 0 auto;
    padding: var(--spacing-xl);
  }

  .page-header {
    margin-bottom: var(--spacing-2xl);
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-xs);
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-bottom: var(--spacing-md);
    transition: color var(--transition-fast);
  }

  .back-link:hover {
    color: var(--text-primary);
  }

  .back-link i {
    width: 16px;
    height: 16px;
  }

  .page-header h1 {
    font-size: 2rem;
    margin-bottom: var(--spacing-sm);
  }

  .text-muted {
    color: var(--text-muted);
  }

  .project-form {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xl);
  }

  .form-section {
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    padding: var(--spacing-xl);
  }

  .form-section h2 {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    font-size: 1.25rem;
    margin-bottom: var(--spacing-lg);
  }

  .step-num {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--accent-gradient);
    border-radius: 50%;
    font-size: 0.9rem;
    font-weight: 700;
    color: #000;
  }

  .section-desc {
    color: var(--text-muted);
    margin-bottom: var(--spacing-lg);
    margin-top: calc(-1 * var(--spacing-sm));
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-lg);
  }

  .form-row-3 {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: var(--spacing-lg);
  }

  .existing-images {
    margin-bottom: var(--spacing-lg);
  }

  .existing-images .form-label {
    margin-bottom: var(--spacing-sm);
  }

  /* Upload Area */
  .upload-area {
    position: relative;
    border: 2px dashed var(--border-color);
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
  }

  .upload-area:hover,
  .upload-area.dragover {
    border-color: var(--accent-primary);
    background: rgba(0, 255, 136, 0.05);
  }

  .file-input {
    position: absolute;
    width: 100%;
    height: 100%;
    opacity: 0;
    cursor: pointer;
  }

  .upload-label {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-3xl);
    text-align: center;
    cursor: pointer;
  }

  .upload-label i {
    width: 48px;
    height: 48px;
    color: var(--text-muted);
  }

  .upload-hint {
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  .file-preview {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-md);
    margin-top: var(--spacing-md);
  }

  .preview-item {
    position: relative;
    width: 100px;
    height: 100px;
    border-radius: var(--radius-md);
    overflow: hidden;
    border: 2px solid var(--border-color);
  }

  .preview-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .preview-item .remove-btn {
    position: absolute;
    top: 4px;
    right: 4px;
    width: 24px;
    height: 24px;
    background: rgba(255, 0, 0, 0.8);
    border: none;
    border-radius: 50%;
    color: white;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity var(--transition-fast);
  }

  .preview-item:hover .remove-btn {
    opacity: 1;
  }

  .preview-item .remove-btn i {
    width: 14px;
    height: 14px;
  }

  /* Priority Sliders */
  .priority-sliders {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xl);
  }

  .priority-item label {
    display: block;
    font-weight: 500;
    margin-bottom: var(--spacing-sm);
  }

  .priority-slider {
    width: 100%;
    height: 8px;
    background: var(--bg-secondary);
    border-radius: var(--radius-full);
    outline: none;
    -webkit-appearance: none;
  }

  .priority-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    width: 20px;
    height: 20px;
    background: var(--accent-gradient);
    border-radius: 50%;
    cursor: pointer;
  }

  .priority-labels {
    display: flex;
    justify-content: space-between;
    font-size: 0.8rem;
    color: var(--text-muted);
    margin-top: var(--spacing-xs);
  }

  /* DIY Option */
  .diy-option {
    display: flex;
    align-items: center;
  }

  .diy-option input[type="checkbox"] {
    display: none;
  }

  .diy-option label {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-md);
    background: var(--bg-secondary);
    border: 2px solid var(--border-color);
    border-radius: var(--radius-lg);
    cursor: pointer;
    transition: all var(--transition-base);
    width: 100%;
  }

  .diy-option input:checked + label {
    border-color: var(--accent-primary);
    background: rgba(0, 255, 136, 0.05);
  }

  .diy-icon {
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg-tertiary);
    border-radius: var(--radius-md);
    color: var(--text-muted);
    flex-shrink: 0;
  }

  .diy-option input:checked + label .diy-icon {
    background: var(--accent-gradient);
    color: #000;
  }

  .diy-option strong {
    display: block;
    font-size: 0.9rem;
    margin-bottom: 2px;
  }

  .diy-option p {
    font-size: 0.8rem;
    color: var(--text-muted);
    margin: 0;
  }

  /* Form Actions */
  .form-actions {
    display: flex;
    justify-content: flex-end;
    gap: var(--spacing-md);
    padding-top: var(--spacing-lg);
  }

  @media (max-width: 768px) {
    .form-row,
    .form-row-3 {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  // Drag and drop upload
  const uploadArea = document.getElementById('upload-area');
  const fileInput = document.getElementById('images');
  const previewContainer = document.getElementById('images-preview');

  ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(eventName => {
    uploadArea.addEventListener(eventName, preventDefaults, false);
  });

  function preventDefaults(e) {
    e.preventDefault();
    e.stopPropagation();
  }

  ['dragenter', 'dragover'].forEach(eventName => {
    uploadArea.addEventListener(eventName, () => uploadArea.classList.add('dragover'), false);
  });

  ['dragleave', 'drop'].forEach(eventName => {
    uploadArea.addEventListener(eventName, () => uploadArea.classList.remove('dragover'), false);
  });

  uploadArea.addEventListener('drop', (e) => {
    const dt = e.dataTransfer;
    const files = dt.files;
    fileInput.files = files;
    fileInput.dispatchEvent(new Event('change'));
  });

  // Image preview
  fileInput.addEventListener('change', function() {
    previewContainer.innerHTML = '';

    if (this.files.length > 5) {
      alert('Maximum 5 images autorisées');
      this.value = '';
      return;
    }

    Array.from(this.files).forEach((file, index) => {
      if (file.size > 10 * 1024 * 1024) {
        alert(`Le fichier ${file.name} est trop volumineux (max 10 Mo)`);
        return;
      }

      const reader = new FileReader();
      reader.onload = function(e) {
        const previewItem = document.createElement('div');
        previewItem.className = 'preview-item';
        previewItem.innerHTML = `
          <img src="${e.target.result}" alt="Preview ${index + 1}">
          <button type="button" class="remove-btn" onclick="removeImage(${index})">
            <i data-lucide="x"></i>
          </button>
        `;
        previewContainer.appendChild(previewItem);
        lucide.createIcons();
      };
      reader.readAsDataURL(file);
    });
  });

  function removeImage(index) {
    const dt = new DataTransfer();
    const files = fileInput.files;

    for (let i = 0; i < files.length; i++) {
      if (i !== index) {
        dt.items.add(files[i]);
      }
    }

    fileInput.files = dt.files;
    fileInput.dispatchEvent(new Event('change'));
  }
</script>
